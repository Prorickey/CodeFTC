{
  "title": "Feedforward Control",
  "testCount": 1,
  "hints": [
    {
      "title": "How to read the encoder position",
      "content": "Use `int currentPosition = armMotor.getCurrentPosition();` to read the encoder. In this simulation, it returns 200."
    },
    {
      "title": "How to calculate the P term",
      "content": "First calculate error: `double error = targetPosition - currentPosition;` (500 - 200 = 300). Then the P term: `double pTerm = Kp * error;` (0.001 * 300 = 0.3)."
    },
    {
      "title": "How to add feedforward",
      "content": "Simply add the gravity constant to the PID output: `double totalPower = pTerm + kG;` This gives 0.3 + 0.1 = 0.4."
    },
    {
      "title": "Why add kG?",
      "content": "The kG constant represents the power needed to hold the arm against gravity. Without it, the arm would sag. The P controller handles getting to the target, while kG handles fighting gravity."
    }
  ]
}
